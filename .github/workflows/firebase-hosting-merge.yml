# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - release
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [14.x]

    steps:
      - uses: actions/checkout@v2
      - run: npm ci && npm run build

      - name: setup todo list app
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - run: npm ci --prefix ./public/todo_list
      - run: npm run build --prefix ./public/todo_list
      - run: mv -v ./public/todo_list/build/* ./public/todo_list
      
      - name: setup poke times app
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - run: npm ci --prefix ./public/poke_times
      - run: npm run build --prefix ./public/poke_times
      - run: mv -v ./public/poke_times/build/* ./public/poke_times

      - name: setup mario plan app
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - run: npm ci --prefix ./public/mario_plan
      - run: npm run build --prefix ./public/mario_plan
      - run: mv -v ./public/mario_plan/build/* ./public/mario_plan
      
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_ZAIO_UPSKILLING }}'
          channelId: live
          projectId: zaio-upskilling
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
